// min_P_in
// max_P_out
// max_Temp
// tagLength


let topic = msg.topic.startsWith(context.get('tag')) ? msg.topic.slice(context.get('tagLength')) : msg.topic;  // удаляем путь из топика, от последней /

switch (topic) {
  case '':
  break;

  case '':
  break;
  
  case '':
  break;
  
  case '':
  break;
  
  case '':
  break;
  
  case '':
  break;
  
  case '':
  break;
  
  case '':
  break;
    
}